Nicholas Stewart
Program 3

Run this program by using the command
python3 classify.py testx.txt stepSize Epsilon MAXCOST randomRestarts -v(optional)

ex.
python3 classify.py test1.txt 0.1 0.01 0.2 100 -v

This program peforms Gradiant Decent once with the centroids of the training set(dynamically calcualted)
no further random exemplars are generated.
This is the only difference I am aware of everything else about the program should be to specifications.
